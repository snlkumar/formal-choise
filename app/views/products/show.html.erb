
 <div class="col-lg-6 product-show">
 	 <script type="text/javascript">
            CloudZoom.quickStart();
        </script>
        
	<!-- <%@product.images.each do |image|%>
	<%=image_tag(image.image.url,height: 500,width: 400) unless image.blank?%>
    <%end%> -->
     <img class = "cloudzoom" src = "<%=@product.images.first.image.url(:small)%>"
             data-cloudzoom = "zoomImage: '<%=@product.images.first.image.url(:large)%>'" />
        <br/>
        <%@product.images.each do |image|%>
        <img class = 'cloudzoom-gallery' src = "<%=image.image.url(:thumb)%>" data-cloudzoom = "useZoom: '.cloudzoom', image: '<%=image.image.url(:small)%>', zoomImage: '<%=image.image.url(:large)%>' " >
       <%end%>
        
</div>
<div class="col-lg-6">
<div class="row">
 <div class="col-lg-10 cufstom-form" >
<div class="well">
	<h4>Name</h4>
	<%=@product.name%>
	
	<h4>Description</h4>
	<p><%=@product.description%></p>
	<h4>Brand</h4>
	<%=@product.brand.name unless @product.brand.blank?%>
	<h4>Color</h4>
	<%=@product.color.name unless @product.color.blank?%>
	<h4>Seasion</h4>
	<%=@product.seasion.name unless @product.seasion.blank?%>
	<h4>Category</h4>	
	<%=@product.category.name unless @product.category.blank? %>
	<h4>Tags</h4>
	<%=@product.tags.count%>   
    
    </div></div></div>
</div> 
<div class="row">
 <div class="col-lg-10 cufstom-form" >
<div class="well">
<h3 class="page-header">Reviews</h3>
 <%unless @product.reviews.blank?%>
 <%@product.reviews.each do |r|%>
 <li style="none;">jjjjkjkkj</li>
 <%end%>
 <%end%>

</div>
</div></div>
